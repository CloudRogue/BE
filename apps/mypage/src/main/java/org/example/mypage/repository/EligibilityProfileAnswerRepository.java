package org.example.mypage.repository;

import org.example.mypage.domain.EligibilityProfileAnswer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EligibilityProfileAnswerRepository extends JpaRepository<EligibilityProfileAnswer, Long> {
}
